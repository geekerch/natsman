# Subscriptions 功能更新

## 問題
之前的訂閱面板沒有提供添加新訂閱的方法，用戶無法直接在面板中訂閱新的 subject。

## 解決方案
添加了完整的訂閱輸入功能。

## 新增功能

### 1. 新訂閱輸入區域
在訂閱面板頂部添加了輸入區域：
- **輸入框**: 用於輸入要訂閱的 subject
- **Subscribe 按鈕**: 點擊開始訂閱
- **Enter 鍵支持**: 在輸入框按 Enter 也可以訂閱

### 2. 訂閱計數顯示
- 顯示當前活躍訂閱數量
- 藍色圓形徽章樣式

### 3. 改進的空狀態提示
當沒有訂閱時，顯示更友好的提示信息。

## UI 佈局

```
┌─────────────────────────────────────────────────────┐
│ Active Subscriptions                       [×]      │
├───────────────────────┬─────────────────────────────┤
│ New Subscription      │ Messages        [🔄] [🗑️]  │
│ ┌─────────────────┐   │                             │
│ │ events.test     │[Subscribe]                      │
│ └─────────────────┘   │                             │
│                       │                             │
│ Active Subscriptions │                             │
│ (2)                   │                             │
│ ┌──────────────────┐  │                             │
│ │ events.test   ❌ │  │                             │
│ └──────────────────┘  │                             │
│ ┌──────────────────┐  │                             │
│ │ user.created  ❌ │  │                             │
│ └──────────────────┘  │                             │
├───────────────────────┴─────────────────────────────┤
│                                [Close]               │
└─────────────────────────────────────────────────────┘
```

## 使用方法

### 方法 1: 在訂閱面板中添加
1. 點擊側邊欄的 "🌐 Subscriptions" 按鈕
2. 在頂部的 "New Subscription" 輸入框中輸入 subject
   - 例如: `events.test`
3. 點擊 "Subscribe" 按鈕（或按 Enter 鍵）
4. 訂閱成功後會出現在下方的列表中

### 方法 2: 從主界面訂閱（已移除）
之前的 Subscribe 按鈕已移除，現在統一在訂閱面板中管理。

## 修改的文件

### HTML (`web/index.html`)
- 添加了 `subscription-input-section` 區域
- 添加了輸入框和按鈕
- 添加了訂閱計數顯示
- 重新組織了訂閱列表結構

### JavaScript (`web/app.js`)
- 新增 `addNewSubscription()` 函數
- 更新 `renderSubscriptionsList()` 顯示計數
- 添加 Enter 鍵事件監聽
- 改進空狀態提示文字

### CSS (`web/style.css`)
- 新增 `.subscriptions-list-wrapper` 樣式
- 新增 `.subscription-input-section` 樣式
- 新增 `.input-group` 樣式
- 新增 `.subscription-count` 徽章樣式
- 添加滾動條美化

## 測試步驟

1. **啟動服務器**
   ```bash
   ./natsman-server
   ```

2. **打開瀏覽器**
   ```
   http://localhost:8080
   ```

3. **測試訂閱功能**
   - 點擊側邊欄 "Subscriptions" 按鈕
   - 在輸入框輸入: `events.test`
   - 點擊 "Subscribe" 按鈕
   - 確認訂閱出現在列表中
   - 確認計數顯示正確

4. **測試發布和接收**
   - 在主界面選擇 Mode: Publish
   - Subject: `events.test`
   - Payload: `{"message": "Hello"}`
   - 點擊 "Publish"
   - 返回訂閱面板查看消息

5. **測試取消訂閱**
   - 點擊訂閱項右側的 ❌ 按鈕
   - 確認訂閱從列表中移除
   - 確認計數更新

## 鍵盤快捷鍵

- **Enter**: 在輸入框中按 Enter 直接訂閱
- **Esc**: 關閉訂閱面板

## 注意事項

1. **Subject 格式**: 支持任何有效的 NATS subject，包括通配符
   - 簡單: `test`
   - 分層: `events.user.created`
   - 通配符: `events.*` 或 `events.>`

2. **重複訂閱**: 如果已經訂閱了該 subject，會提示錯誤

3. **連接配置**: 使用當前 Settings 中配置的 NATS 連接

## 改進效果

### 之前 ❌
- 沒有直接訂閱的方法
- 需要在主界面輸入 subject 後才能訂閱
- 用戶體驗不直觀

### 現在 ✅
- 在訂閱面板直接添加新訂閱
- 清晰的輸入界面
- 實時計數顯示
- 一鍵操作
- 支持 Enter 鍵快速訂閱

## 未來可能的增強

- [ ] 保存常用訂閱列表
- [ ] 訂閱模板（預設的 subject 列表）
- [ ] 批量訂閱
- [ ] 訂閱歷史記錄
- [ ] 搜索和過濾訂閱

## 相關功能

- **Publish**: 在主界面選擇 Mode: Publish 發布消息
- **查看消息**: 點擊訂閱項查看實時消息
- **刷新消息**: 自動每 2 秒刷新，或手動點擊刷新按鈕
- **清空消息**: 點擊清空按鈕清除消息歷史
- **取消訂閱**: 點擊 ❌ 按鈕取消訂閱

## 故障排除

### 訂閱失敗
**可能原因**: NATS 服務器未運行
**解決方案**: 確保 NATS server 正在運行
```bash
nats-server
```

### 沒有收到消息
**可能原因**: Subject 不匹配
**解決方案**: 確保發布和訂閱使用相同的 subject

### 輸入框無反應
**可能原因**: JavaScript 錯誤或瀏覽器緩存
**解決方案**: 
1. 按 F12 打開開發者工具查看錯誤
2. 強制刷新頁面 (Ctrl+Shift+R)
3. 清除瀏覽器緩存

## 完成狀態

✅ 所有功能已實現並測試通過
✅ UI 美觀且易用
✅ 支持鍵盤快捷鍵
✅ 錯誤處理完善
✅ 用戶體驗流暢
